/**
 * @param {number} x
 * @return {number}
 */
var mySqrt = function (x) {
  let min = 0
  let max = x
  let a = 0
  // for (let i = 1; i < x; i++){
  //   if (i * i == x) {
  //     return i
  //   }//排除整数的可能

  // }
  while (a * a != x) {
    let b = a / 2
    if (b * b > x) {

    }
  }
};
