/**
 * @param {string} s
 * @return {boolean}
 */
var isValid = function (s) {
  let a = []
  for (let i = 0; i < s.length; i++) {
    a.push(s[i])
  }
  if (a[j] == "(" || a[j] == "(" || a[j] == "(" || a[j] == "(" || a[j] == "(" || ) {
    for (let j = 0; j < a.length; j++) {
    }
  };
}
